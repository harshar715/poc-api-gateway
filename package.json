{
  "name": "api-gateway-sample-app",
  "version": "1.0.0",
  "description": "Sample API Gateway application with GET and POST endpoints for POC",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api-gateway",
    "lambda",
    "aws",
    "poc"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.490.0",
    "@aws-sdk/lib-dynamodb": "^3.490.0"
  }
}

